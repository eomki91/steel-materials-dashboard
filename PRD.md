# PRD — 철강 원자재 가격 대시보드 (가칭 SteelPulse)

작성일: 2026-09-01 · 버전: MVP 1.0 · 개발 규모: 1인 / 2주

---

## 1. 앱의 목적

철강업계 실무자가 **아침에 30초 안에** 오늘의 원자재 시장 상황을 파악할 수 있게 한다.

여러 사이트(스틸데일리, 뉴스, 공공 통계)를 돌아다니며 확인하던 스크랩·철광석·석탄 가격과 관련 뉴스를 **한 화면에 모으고**, AI가 3~5줄로 요약해 준다.

원칙:
- 가격을 예측하거나 매매를 조언하지 않는다. **현재 상태를 보여주기만 한다.**
- 모든 숫자에 **출처와 기준일**을 함께 표시한다. 출처를 밝힐 수 없는 숫자는 화면에 올리지 않는다.
- 데이터를 임의로 생성하거나 보간하지 않는다. 값이 없으면 "데이터 없음"으로 표시한다.

## 2. 주요 사용자

| 사용자 | 상황 | 필요한 것 |
|---|---|---|
| **제강사 원료 구매 담당자** (1차 타깃) | 매일 아침 스크랩 시세와 원료 동향을 확인해야 함 | 스크랩 가격 변동, 철광석·석탄 방향성 |
| **철강 유통·트레이딩 담당자** | 매입/매도 가격 협상 전 시장 분위기 파악 | 최근 추이 차트, 근거가 되는 뉴스 |
| **기획·전략 담당자** | 주간 보고서에 넣을 시황 정리 | AI 요약문, 출처가 붙은 수치 |

공통점: **철강 도메인 지식은 있고 데이터 도구는 안 쓴다.** 필터·설정 없이 열자마자 보여야 한다.

## 3. 핵심 기능

| # | 기능 | 설명 |
|---|---|---|
| F1 | **스크랩 가격** | 국내 철스크랩 기준가격. 최신값 + 전일/전주 대비 증감 |
| F2 | **철광석 가격** | 62% Fe, CFR China 기준 |
| F3 | **석탄 가격** | 원료탄(호주 FOB) 또는 유연탄 기준 |
| F4 | **추이 차트** | 각 품목의 최근 6~12개월 선형 차트 |
| F5 | **관련 뉴스** | 철강/스크랩/철광석 키워드 최신 뉴스 10건 (제목·출처·날짜·링크) |
| F6 | **AI 시황 요약** | 가격 변동 + 뉴스 헤드라인을 근거로 3~5줄 요약 |
| F7 | **출처·기준일 표시** | 모든 카드·차트·요약에 출처명, 출처 링크, 기준일, 수집 시각 표기 |

**제외 (MVP 범위 밖):** 회원가입/로그인, 결제, 알림, 권한관리, 관리자 페이지, 사용자별 설정 저장, 가격 예측, 다국어.

## 4. 화면 구성

**단일 페이지, 세로 스크롤.** 탭이나 사이드바 메뉴 없음.

```
┌──────────────────────────────────────────────────────────────┐
│  🔩 철강 원자재 대시보드      마지막 갱신: 2026-09-01 09:12   │
│                                            [ 새로고침 ]      │
├──────────────────────────────────────────────────────────────┤
│  ── AI 시황 요약 ────────────────────────────────────────    │
│  국내 철스크랩은 9월 첫째 주 들어 톤당 1만 원 내렸다.        │
│  철광석은 8월 평균 대비 보합권이며 ...                        │
│  ⓘ Claude Opus 5 생성 · 아래 가격/뉴스 데이터 기반           │
├──────────────────────────────────────────────────────────────┤
│  ┌─ 철스크랩 ──┐ ┌─ 철광석 ────┐ ┌─ 석탄 ──────┐            │
│  │ 435,000원/t │ │ $102.4/t    │ │ $187.5/t    │            │
│  │ ▼ 10,000    │ │ ▲ 1.2       │ │ ─ 0.0       │            │
│  │ 2026-08-31  │ │ 2026-08월   │ │ 2026-08월   │            │
│  │ 출처: ○○ ↗ │ │ 출처: ○○ ↗ │ │ 출처: ○○ ↗ │            │
│  └─────────────┘ └─────────────┘ └─────────────┘            │
├──────────────────────────────────────────────────────────────┤
│  ── 최근 추이 ───────────────────────────────────────────    │
│  [ 철스크랩 | 철광석 | 석탄 ]  ← 라디오 3개                   │
│  ┌────────────────────────────────────────────────┐          │
│  │              선형 차트 (최근 12개월)            │          │
│  └────────────────────────────────────────────────┘          │
│  기간: 2025-09 ~ 2026-08 · 출처: ○○ · 수집 09-01 09:12      │
├──────────────────────────────────────────────────────────────┤
│  ── 관련 뉴스 ───────────────────────────────────────────    │
│  · 9월 첫째 주, 국내 철스크랩價 하락       스틸데일리 08-31 ↗│
│  · 환영철강, 철스크랩 매입 가격 인하…톤당 1만 원  ○○ 08-28 ↗│
│  · … (총 10건)                                                │
├──────────────────────────────────────────────────────────────┤
│  데이터 출처 안내 / 면책 문구 / 수집 시각                      │
└──────────────────────────────────────────────────────────────┘
```

**표시 규칙**
- 값이 없으면 `—` 와 "데이터 없음 (사유)"을 표시한다. 이전 값을 끌어다 쓰지 않는다.
- 기준일이 오늘이 아니면 "8월 평균", "08-31 기준"처럼 **실제 기준 시점을 그대로** 쓴다.
- 단위(원/톤, USD/톤)와 기준(62% Fe, CFR China)을 카드에 항상 병기한다.
- 뉴스는 **제목 + 링크 + 출처 + 날짜만** 노출한다. 본문을 복제하지 않는다.

## 5. 데이터 수집 방법

### 5.1 소스 조사 결과 (2026-09-01 실제 확인)

| 소스 | 대상 | 상태 | 판단 |
|---|---|---|---|
| **Google News RSS** | 뉴스 (F5) | ✅ **확인 완료** — 정상 응답, `title`/`link`/`pubDate`/`source` 모두 포함 | **MVP에서 사용** |
| **World Bank Pink Sheet** | 철광석·석탄 (F2, F3) | ✅ **확인 완료** — 월간 Excel/PDF 공개. 철광석(any origin, fines, spot CFR China, 62% Fe), 석탄(호주 뉴캐슬 6,000kcal / 남아공 리처즈베이) 시계열 포함 | **MVP 기준 소스** (월간) |
| **KOMIS (한국자원정보서비스)** | 철광석·유연탄 (F2, F3) | ⚠️ **부분 확인** — 6대 전략광종(유연탄·철광석 포함) 가격을 무료 공개하나, 표가 **JS로 동적 로드**됨. 페이지 HTML에 값이 없음 | Day 0에 내부 JSON 엔드포인트 확인 후 채택 여부 결정 |
| **스틸데일리** | 스크랩 가격 (F1) | ⚠️ **가격표는 비공개** — 기사 헤드라인·카테고리는 공개, 가격 데이터는 구독 영역으로 보임 | **가격표 스크래핑 안 함.** 뉴스 출처로만 활용 |
| **제강사 매입가 공지** | 스크랩 가격 (F1) | ❓ **미확인** | Day 0 조사 대상 |

> **국내 철스크랩의 무료·공개 시계열 API는 현재 확인되지 않았다.** 이것이 이 프로젝트의 유일한 실질적 리스크다. 없는 API를 가정하고 설계하지 않기 위해, F1은 아래 폴백 전략으로 간다.

### 5.2 품목별 수집 전략

**F1 철스크랩 (Day 0에 A → B → C 순으로 결정)**
- **A안** — 제강사(현대제철·동국제강·환영철강 등) 홈페이지의 매입가격 공지 페이지가 정적 HTML이면 파싱한다.
- **B안** — 뉴스 본문에서 가격을 추출하지 **않고**, 관리자가 주 1~2회 `data/scrap_manual.csv`에 직접 입력한다. 각 행에 출처 URL과 기준일을 필수로 적는다. 화면에는 "수기 입력 · 출처: (링크)"로 명시한다.
- **C안** — A/B 모두 불가하면 **F1 카드에 "데이터 소스 확보 중"을 표시**하고 F2·F3만으로 출시한다. 가짜 값을 넣지 않는다.
- MVP 기본값은 **B안**. 가장 확실하고, 출처가 투명하며, 하루면 만든다.

**F2 철광석 / F3 석탄**
- World Bank Pink Sheet 월간 Excel을 다운로드해 해당 시리즈 열만 파싱한다. 월간이므로 차트는 월 단위 선형 차트가 된다.
- KOMIS 엔드포인트가 Day 0에 확인되면 일간 데이터로 교체하거나 보조 소스로 병기한다.

**F5 뉴스**
- Google News RSS 검색 피드를 `feedparser`로 읽는다. 쿼리: `철스크랩`, `철광석 가격`, `원료탄`, `철강 시황`.
- 쿼리별로 받아 제목 기준 중복 제거 후 `pubDate` 내림차순 상위 10건.

**F6 AI 요약**
- 위에서 **수집이 끝난 데이터만** 프롬프트에 넣는다(가격 3종의 최신값·증감·기준일 + 뉴스 제목 10건).
- 모델이 웹을 다시 검색하거나 수치를 지어내지 않도록 시스템 프롬프트에 "주어진 데이터에만 근거하고, 없는 값은 언급하지 말 것"을 명시한다.
- 요약 결과를 캐시해 재실행 때 매번 호출하지 않는다.

### 5.3 수집 정책

- **캐시:** `@st.cache_data(ttl=3600)` — 가격 1시간, 뉴스 30분, AI 요약 6시간.
- **매너:** 요청 간 1초 간격, `User-Agent` 명시, `robots.txt` 준수. 유료/구독 영역은 접근하지 않는다.
- **실패 처리:** 소스 하나가 죽어도 앱 전체가 죽지 않는다. 해당 카드만 "수집 실패 (사유)"로 표시하고 나머지는 렌더링한다.
- **이력:** 매 수집 성공 시 `data/prices.csv`에 append 한다. 스크래핑 실패에 대비한 로컬 히스토리 축적 목적.

## 6. 데이터 구조

파일 기반. DB 없음.

**`data/prices.csv`** — 모든 가격의 단일 테이블 (long format)

| 컬럼 | 타입 | 예시 | 설명 |
|---|---|---|---|
| `date` | date | `2026-08-31` | 가격 **기준일** (수집일 아님) |
| `item` | str | `scrap` \| `iron_ore` \| `coal` | 품목 코드 |
| `value` | float | `435000` | 가격 |
| `unit` | str | `KRW/t`, `USD/t` | 단위 |
| `spec` | str | `62% Fe, CFR China` | 기준 규격 |
| `source_name` | str | `World Bank Pink Sheet` | 출처명 (화면 표시용) |
| `source_url` | str | `https://...` | 출처 링크 |
| `collected_at` | datetime | `2026-09-01T09:12:00` | 수집 시각 |

**`data/news.csv`**

| 컬럼 | 예시 |
|---|---|
| `published_at` | `2026-08-31T10:00:00` |
| `title` | `9월 첫째 주, 국내 철스크랩價 하락` |
| `url` | `https://news.google.com/...` |
| `source_name` | `스틸데일리` |
| `query` | `철스크랩` |

**`data/scrap_manual.csv`** — B안 수기 입력용. `date, value, unit, spec, source_name, source_url, note`

**`data/summary_cache.json`**
```json
{
  "generated_at": "2026-09-01T09:12:00",
  "input_hash": "a1b2c3...",
  "text": "국내 철스크랩은 9월 첫째 주 들어 ...",
  "model": "claude-opus-5"
}
```
`input_hash`(가격+뉴스 스냅샷의 해시)가 같으면 재호출하지 않는다.

## 7. 사용할 기술

| 영역 | 선택 | 비고 |
|---|---|---|
| 언어 | Python 3.11+ | |
| UI | **Streamlit** | `st.metric`, `st.line_chart`, `st.columns`, `st.radio` |
| 수집 | `requests`, `beautifulsoup4`, `feedparser` | `feedparser`는 Google News RSS 파싱 |
| Excel 파싱 | `pandas` + `openpyxl` | World Bank Pink Sheet용 |
| 데이터 처리 | `pandas` | |
| 차트 | `st.line_chart` (기본) | 부족하면 `plotly` |
| AI 요약 | **`anthropic` SDK + `claude-opus-5`** | 아래 참고 |
| 저장 | CSV / JSON 파일 | DB 없음 |
| 스케줄링 | 없음 — `st.cache_data(ttl=...)` | 사용자가 열 때 갱신 |
| 설정 | `.env` (로컬) / `st.secrets` (배포) | `ANTHROPIC_API_KEY` |
| 배포 | Streamlit Community Cloud | 무료, GitHub 연동 |

**AI 요약 호출 형태**

```python
import anthropic

client = anthropic.Anthropic()  # ANTHROPIC_API_KEY를 환경에서 읽음

response = client.messages.create(
    model="claude-opus-5",
    max_tokens=1024,
    system=(
        "너는 철강 시황 애널리스트다. 아래 제공된 가격 데이터와 뉴스 제목에만 근거해 "
        "3~5문장으로 요약하라. 제공되지 않은 수치나 사건은 절대 언급하지 마라. "
        "가격 예측이나 매매 조언은 하지 마라."
    ),
    messages=[{"role": "user", "content": prompt_from_collected_data}],
)
summary = next(b.text for b in response.content if b.type == "text")
```

- 요약은 짧은 단일 호출이라 thinking·streaming이 필요 없다.
- 비용이 부담되면 `model="claude-sonnet-5"`로 바꿔도 이 작업엔 충분하다. 호출은 사용자 방문당 최대 하루 4회(6시간 캐시) 수준.
- `response.content`는 블록 리스트다. `.type == "text"`를 확인하고 꺼낸다.

**의존성 (`requirements.txt`)**
```
streamlit
pandas
requests
beautifulsoup4
feedparser
openpyxl
anthropic
python-dotenv
```

## 8. MVP 개발 순서

각 단계가 끝날 때마다 앱이 **실행되는 상태**를 유지한다.

| 단계 | 내용 | 산출물 | 예상 |
|---|---|---|---|
| **0** | **데이터 소스 검증** — Streamlit 코드 이전에 이것부터. 브라우저 개발자도구로 KOMIS의 XHR 엔드포인트를 확인하고, World Bank Pink Sheet Excel을 실제로 받아 시리즈 열 위치를 특정하고, 제강사 매입가 공지 페이지의 정적/동적 여부를 본다. **각 소스별로 "실제로 값이 나오는 최소 스크립트"를 하나씩 작성해 통과시킨다.** 스크랩은 A/B/C 중 무엇으로 갈지 여기서 확정한다. | `scripts/probe_*.py` 3~4개 | 1일 |
| **1** | 수집 모듈 — 0단계 스크립트를 `collectors/` 아래 함수로 정리. 모두 동일한 `prices.csv` 스키마(date/item/value/unit/spec/source_name/source_url/collected_at)를 반환하게 한다. | `collectors/scrap.py`, `iron_ore.py`, `coal.py`, `news.py` | 2일 |
| **2** | 최소 화면 — 가격 카드 3개 + 출처/기준일. 여기서 이미 "쓸 수 있는 앱"이 된다. | `app.py` v0.1 | 1일 |
| **3** | 추이 차트 — 품목 라디오 + `st.line_chart`. 기간·출처 캡션. | v0.2 | 1일 |
| **4** | 뉴스 섹션 — RSS 10건, 중복 제거, 링크. | v0.3 | 0.5일 |
| **5** | AI 요약 — 프롬프트 구성, 캐시(`input_hash`), 실패 시 섹션 숨김. | v0.4 | 1일 |
| **6** | 견고화 — 소스별 try/except 격리, "데이터 없음" 상태 표시, 캐시 TTL 조정, 새로고침 버튼. | v0.9 | 1일 |
| **7** | 배포 — Streamlit Community Cloud, `st.secrets`에 API 키, 면책 문구 추가. | v1.0 | 0.5일 |

**총 8~9일.** 0단계가 가장 중요하다. 여기서 소스가 확보되지 않으면 나머지 단계를 아무리 잘 만들어도 빈 대시보드가 된다.

**출시 판정 기준 (MVP Done)**
- [ ] 철광석·석탄 최신값이 출처·기준일과 함께 표시된다
- [ ] 스크랩은 실제 값이 표시되거나, "소스 확보 중"이 명시된다 (가짜 값 없음)
- [ ] 3개 품목 모두 추이 차트가 그려진다 (데이터가 있는 품목에 한해)
- [ ] 뉴스 10건이 링크와 함께 뜬다
- [ ] AI 요약이 실제 수집 데이터만 근거로 생성된다
- [ ] 소스 하나를 강제로 실패시켜도 앱이 죽지 않는다

---

### 면책 문구 (푸터에 표시)

> 본 대시보드는 공개된 데이터를 수집해 참고용으로 제공합니다. 각 수치의 정확성은 원 출처를 확인하시기 바라며, 투자·매매 판단의 근거로 사용하지 마십시오. AI 요약은 자동 생성된 것으로 오류가 있을 수 있습니다.
